let name = prompt("What is the 'oficial' name of JS?");
let message;

if (name === "ECMAScript") {
  message = "That's correct!";
} else message = "You don't know? It is ECMAScript!";

alert(message);
